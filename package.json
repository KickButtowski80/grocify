{
	"name": "my-project",
	"source": "src/index.html",
	"scripts": {
		"dev": "rm -rf .parcel-cache .cache dist && parcel",
		"build": "npx parcel src/index.html",
		"clean": "rm -rf dist/*"
	},
	"dependencies": {
		"parcel": "^2.9.3"
	},
	"devDependencies": {
		"postcss": "^8.4.27",
		"posthtml-include": "^1.7.4",
		"tailwindcss": "^3.3.3"
	}
}